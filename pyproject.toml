[project]
name = "scribe"
version = "0.1.0"
description = "Scribe: An AI-powered assistant for the secretary of the Mercy Ridge Residents Council"
authors = [{ name = "George Wright", email = "user3@example.com" }]
requires-python = ">=3.10,<3.14"

dependencies = [
    "crewai[tools]>=0.134.0,<1.0.0",
    "flask>=2.3.0,<3.0.0",
    "jinja2>=3.1.0",
    "google-auth>=2.0.0",
    "google-auth-oauthlib>=1.0.0",
    "google-api-python-client>=2.0.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
    "openai>=1.0.0",
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "pypdf>=4.0",
    "pypdfium2>=5.0.0",
    "reportlab>=4.0",
    "beautifulsoup4>=4.12",
    "tqdm>=4.66",
    "unstructured>=0.11.0",
    "chromadb>=0.4.0",
    "psycopg2-binary>=2.9.11",
    "sqlalchemy>=2.0.0",
    "alembic>=1.13.0",
    "pydantic-settings>=2.0.0",
]

[project.scripts]
scribe = "scribe.main:run"
run_crew = "scribe.main:run"
train = "scribe.main:train"
replay = "scribe.main:replay"
test = "scribe.main:test"
scribe-ingest = "scribe.cli:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.envs.default]
dependencies = [
    "pytest",
    "black",
    "mypy",
    "ipython"
]

[tool.crewai]
type = "crew"
